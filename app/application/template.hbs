<div class="alerts">
  {{#each flashMessages.queue as |flash|}}
    {{#flash-message flash=flash as |component message|}}
      <div class="message is-{{lowercase component.flashType}}">
        <div class="message-header">{{component.flashType}}</div>
        <p class="message-body">
          {{message.message}}
        </p>
      </div>
    {{/flash-message}}
  {{/each}}
</div>
{{outlet}}
