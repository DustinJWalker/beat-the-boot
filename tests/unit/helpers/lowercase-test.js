
import { lowercase } from 'beat-the-boot/helpers/lowercase';
import { module, test } from 'qunit';

module('Unit | Helper | lowercase');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lowercase([42]);
  assert.ok(result);
});

